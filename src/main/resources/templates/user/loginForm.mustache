<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>루틴 관리 로그인</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
        background: linear-gradient(135deg, #f2fff2, #e7fbe7);
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .login-box {
        width: 100%;
        max-width: 380px;
        background-color: white;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
      }

      .login-logo {
        text-align: center;
        margin-bottom: 1.5rem;
      }

      .login-logo h1 {
        font-weight: bold;
        font-size: 1.8rem;
        color: #2d2d2d;
      }

      .login-logo .icon {
        font-size: 2.2rem;
        color: #2ecc71;
      }

      .form-check-label {
        font-size: 0.9rem;
      }

      .btn-login {
        background-color: #2ecc71;
        color: white;
        font-weight: 600;
      }

      .btn-login:hover {
        background-color: #27ae60;
      }

      .btn-kakao {
        background-color: #fee500;
        color: #000;
        font-weight: 600;
        border: 1px solid #e0c000;
      }

      .btn-kakao img {
        height: 20px;
        margin-right: 6px;
      }

      .bottom-link {
        margin-top: 1rem;
        text-align: center;
        font-size: 0.9rem;
      }

      .bottom-link a {
        text-decoration: none;
        color: #2ecc71;
        font-weight: 500;
      }
    </style>
  </head>
  <body>
    <div class="login-box">
      <h1>WR</h1>
      <form>
        <div class="mb-3">
          <input type="email" class="form-control" placeholder="이메일 주소" required />      
        </div>
        <div class="mb-3">
          <input type="password" class="form-control" placeholder="비밀번호" required />
        </div>
        {{! <div class="form-check mb-3">
          <input type="checkbox" class="form-check-input" id="rememberMe" />
          <label class="form-check-label" for="rememberMe">로그인 상태 유지</label> 
        </div> }}
        <div class="d-grid mb-2">
          <button type="submit" class="btn btn-login">로그인</button>
        </div>
        <div class="d-grid">
          <button type="button" class="btn btn-kakao">
            <img src="https://developers.kakao.com/assets/img/about/logos/kakaolink/kakaolink_btn_medium.png" alt="kakao" />Kakao 로그인
          </button>
        </div>
      </form>
      <div class="bottom-link">
        아직 계정이 없으신가요?
        <a href="">회원가입</a>
      </div>
    </div>
  </body>
</html>
